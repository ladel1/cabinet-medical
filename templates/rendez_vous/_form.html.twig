<div class="row mt-5">
    <div class="col-6 offset-3">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            

            <div class="mb-3 row">
                <label class="col-form-label col-sm-2 required" for="rendez_vous_medecin">Semaine</label>
                <div class="col-sm-10">
                    <input type="text" id="selector" class="form-control" name="" id="">
               </div>
            </div>
            <div id="ava">
                <div class="alert alert-primary">
                    Pour avoir les désponibilités sélectionnez un médecin et une semaine :) 
                </div>
                {# {{ include("rendez_vous/_table_availability.html.twig",{'selectedDay':"now","availabilities":[]}) }}   #}
            </div>
            <button class="btn btn-primary mb-5">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
</div>

 
