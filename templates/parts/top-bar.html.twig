<div class="row">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="{{path('app_home')}}">
        <img class="logo" src="{{asset('img/med.jpg')}}" alt="Logo">
        Cabinet Médical</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav me-auto">
          <li class="nav-item active">
            <a class="nav-link" href="{{path('app_home')}}">Accueil
              <span class="visually-hidden">(current)</span>
            </a>
          </li>
          {% if not app.user %}
            <li class="nav-item">
              <a class="nav-link" href="{{path("app_register")}}">Inscription</a>
            </li>  
            <li class="nav-item">
              <a class="nav-link" href="{{path("app_login")}}">Connexion</a>
            </li>  
          {% else %}

            {% if is_granted("ROLE_MEDECIN") %}
              <li class="nav-item">
                  <a class="nav-link" href="{{path('app_availability_new')}}">Ajouter Disponibilités</a>
              </li>
            {% endif %}
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Gérer Rendez-Vous</a>
              <div class="dropdown-menu">
                <a class="dropdown-item" href="">Mes rendez-vous</a>
                {% if is_granted("ROLE_PATIENT") %}
                  <a class="dropdown-item" href="{{path('app_rendezvous_add') }}">Ajouter un rendez-vous</a>              
                {% endif %}
              </div>
            </li> 
            <li class="nav-item">
              <a class="nav-link" href="{{path("app_logout")}}">Déconnexion</a>
            </li>           
          {% endif %}
        </ul>
        {% if app.user %}
          <div class="d-flex">
            <a class="profil" href="{{ path('app_profil') }}" ><i class="fa-solid fa-user"></i> {{ app.user.username }} </a>
          </div>
        {% endif %}
      </div>
    </div>
  </nav>  
</div>